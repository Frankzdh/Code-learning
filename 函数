def function(a,b):

使用def定义函数，并调用 其中a，b为形参 调用方式:def function(3,4)其中3，4为实参。


函数内部的局部变量，作用域仅在函数内部。否则要定义全局变量。

函数部分，a byte of python 主要讲的是定义函数以及函数的的局部变量，全局变量与默认参数，形参，实参，可变参数等。

还有函数的返回。return 一个函数最后总有个return，及中断函数。



包（组织模块）——模块（重复使用函数）——函数——变量和常量，运算符表达式等
